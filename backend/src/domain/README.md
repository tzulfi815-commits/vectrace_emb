# Domain layers

Phase 2 adds entity types, repository interfaces, in-memory repositories, services, authorization middleware, and REST routes here. Route handlers will only translate HTTP concerns; all Vectrace Emb rules remain in services.
